{
   "main": {
      "enable": true
   },
   "admin": {
      "enable": true
   },
   "user": {
      "enable": true
   },
   "db": {
      "enable": true,
      "package": "ioa-db",
      "config": {
         "default": {
            "host": "localhost",
            "database": "query",
            "username": "postgres",
            "password": "postgres"
         }
      }
   },
   "auth": {
      "enable": true,
      "package": "ioa-auth",
      "config":{
         "default": {
            "authKey": "test"
         }
      }
   },
   "cors": {
      "enable": true,
      "package": "ioa-cors"
   }
}